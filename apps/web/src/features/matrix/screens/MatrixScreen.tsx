import { FeaturePlaceholder } from "@/components/FeaturePlaceholder";

export function MatrixScreen() {
  return (
    <FeaturePlaceholder
      title="Matrix"
      description="Classify tasks by importance and urgency into Q1–Q4. A decision tool, not the whole product."
    />
  );
}
