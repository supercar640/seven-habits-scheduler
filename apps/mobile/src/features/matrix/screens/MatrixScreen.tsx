import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export function MatrixScreen() {
  return (
    <ScreenPlaceholder
      title="Matrix"
      description="Classify tasks by importance and urgency into Q1–Q4."
    />
  );
}
