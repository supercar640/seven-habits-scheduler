import { FeaturePlaceholder } from "@/components/FeaturePlaceholder";

export function PlannerPageScreen() {
  return (
    <FeaturePlaceholder
      title="Planner"
      description="The heart of the app: plan your week around Q2 priorities, then execute daily."
    />
  );
}
