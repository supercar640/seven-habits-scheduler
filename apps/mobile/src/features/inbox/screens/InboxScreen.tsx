import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export function InboxScreen() {
  return (
    <ScreenPlaceholder
      title="Inbox"
      description="Collect tasks here before classifying them."
    />
  );
}
