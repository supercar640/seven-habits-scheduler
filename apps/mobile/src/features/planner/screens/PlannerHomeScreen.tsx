import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export function PlannerHomeScreen() {
  return (
    <ScreenPlaceholder
      title="Planner"
      description="Plan your week around Q2 priorities, then execute daily."
    />
  );
}
